{
  "name": "@repo/service",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.60",
    "@cloudflare/workers-types": "^4.20250819.0",
    "prettier": "3.6.2",
    "typescript": "^5.5.2",
    "wrangler": "^4.40.0"
  },
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "dev": "wrangler types && clear && wrangler dev --port 8787",
    "format": "prettier --write .",
    "test": "vitest --run --config vitest.config.ts",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@erebus-sh/sdk": "^0.0.177",
    "@repo/schemas": "workspace:*",
    "@repo/shared": "workspace:*",
    "@repo/web": "workspace:*",
    "cloudflare": "^4.5.0",
    "hono": "^4.9.6",
    "jose": "^6.0.12",
    "ky": "^1.8.2",
    "nanoid": "^5.1.5",
    "ulid": "^3.0.1",
    "vitest": "~3.2.0",
    "zod": "^4.0.17"
  }
}
